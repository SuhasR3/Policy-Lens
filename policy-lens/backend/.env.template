# Google AI (Gemini) — required for Ask AI (/api/ai/chat) and policy ingest.
# Copy to .env:  cp .env.template .env
# Create a key: https://aistudio.google.com/apikey  →  paste below, then restart uvicorn.
GOOGLE_API_KEY=
